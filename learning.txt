1 . ARG UID=1000
    ARG GID=1000
    this type or parametrs in dockerfile is used only in build time they are like parameters in bash they did not store permanent unless they copy in env 
    they run using --build-arg flag during docker build command 

2 . env varibles are used in both run time and build time 
    they permanently store in image until they are overwrite

these are some common depnedencies which use in dockerfile 

🔹 1. Database related

libpq-dev → PostgreSQL ke liye (Python ka psycopg2)
default-libmysqlclient-dev → MySQL ke liye (Python ka mysqlclient)
sqlite3 + libsqlite3-dev → SQLite ke liye

🔹 2. Image / Video Processing

libjpeg-dev → JPEG support (Pillow, OpenCV)
zlib1g-dev → PNG/GIF compression (Pillow)
libpng-dev → PNG images ke liye
libtiff-dev → TIFF image files
ffmpeg → Audio / Video processing (Django + Media apps)

🔹 3. Security / Encryption

libssl-dev → SSL/TLS (cryptography, OpenSSL, Django/Flask secure cookies)
libffi-dev → Python cryptography libraries (paramiko, bcrypt, etc.)

🔹 4. Python / Machine Learning

python3-dev → Python headers (C extensions compile karne ke liye)
build-essential → GCC compiler tools (numpy, pandas, lxml jaise heavy libs ke liye)
libatlas-base-dev → NumPy / SciPy ke liye (fast linear algebra)
libopencv-dev → OpenCV computer vision ke liye

🔹 5. Node / Frontend

nodejs (agar image me Node pre-installed nahi hai)
npm / yarn → JS/React/Angular build tools
sass / dart-sass → SCSS to CSS conversion (frontend assets)

🔹 6. Web Servers / Runtimes

gunicorn (Python WSGI server for Flask/Django)
uvicorn + fastapi (Python ASGI server for modern apps)
nginx → Static files serve karne / reverse proxy ke liye

🔹 Bonus: Misc Useful
curl → APIs test karne aur downloads ke liye
git → Agar repo se code pull karna hai

tzdata → Timezone setup karne ke liye


🔹 Identify Linux Distro in Container
docker exec -it <container> sh → enter container shell
cat /etc/os-release → distro info
docker inspect <container> --format '{{.Config.Image}}' → base image name from host

🔹 Python Images (Size Optimization)
python:3.6 → based on Debian Buster (large size ~900MB)
python:3.6-slim → smaller Debian (~80–100MB)
python:3.6-alpine → minimal Alpine (~30MB), best for lightweight but some libs may break

🔹 ENTRYPOINT vs CMD
CMD → default command (overridable)
ENTRYPOINT → fixed program (cannot be overridden easily, only args can be changed)
Final command = ENTRYPOINT + CMD
ENTRYPOINT → executable
CMD → default args

🔹 sleep Command (Linux)
sleep 100 → pauses execution for 100 seconds
Units: s (seconds, default), m (minutes), h (hours), d (days)
Use cases: debugging, container keeping alive, script delays

🔹 Find Container Subnet
Inside container:
ip addr show eth0 → shows IP + subnet
ip route → shows default gateway + subnet
From host:
docker inspect <container> → find network
docker network inspect <network> → shows Subnet and Gateway

🔹 Create Custom Docker Network
docker network create \
  --driver bridge \
  --subnet=182.18.0.0/24 \
  --gateway=182.18.0.1 \
  wp-mysql-network
Verify:
docker network ls → list networks
docker network inspect wp-mysql-network → details (subnet + gateway)
Containers attached to same custom network can talk directly.


# To push image in youre docker registry 
first you login your registry
then tag like this : docker tag myapp:latest <registry-username>/<repository-name>:<tag>
docker tag myapp:latest myusername/myapp:1.0

push the image like this : docker push <registry-username>/<repository-name>:<tag>
docker push myusername/myapp:1.0

